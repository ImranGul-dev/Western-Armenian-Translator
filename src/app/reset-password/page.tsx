"use client";
import {useState} from "react"; import {useRouter} from "next/navigation"; import {SiteFrame} from "@/components/SiteFrame"; import {getSupabaseBrowserClient} from "@/lib/supabase/client";
export default function ResetPassword(){const[p,setP]=useState("");const[m,setM]=useState("");const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();const{error}=await getSupabaseBrowserClient().auth.updateUser({password:p});if(error)setM(error.message);else{setM("Password updated.");setTimeout(()=>router.push("/dashboard"),800);}}return <SiteFrame compact><section className="auth-card"><h1>Choose a new password</h1><form className="auth-form" onSubmit={submit}><label>New password<input type="password" minLength={8} required value={p} onChange={e=>setP(e.target.value)}/></label><button className="primary-button">Update password</button>{m&&<p>{m}</p>}</form></section></SiteFrame>}
