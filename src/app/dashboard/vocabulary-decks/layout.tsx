import type {
  ReactNode,
} from "react";

import "./vocabulary-decks-polish.css";


export default function VocabularyDecksLayout({
  children,
}: {
  children: ReactNode;
}) {
  return (
    <>
      {children}
    </>
  );
}
